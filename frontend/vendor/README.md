# Frontend vendor directory

`install.sh` downloads the pinned Three.js **r180** browser modules into this directory:

- `three.module.js`
- `three.core.js`

They are fetched from the official `mrdoob/three.js` GitHub repository and are not committed into Robot Web Lab's source archive. After installation the app works without a CDN or frontend build service.
